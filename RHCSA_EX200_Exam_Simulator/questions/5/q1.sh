#!/bin/bash
# Objective 4: Configure local storage
# Question 1: GPT partitions

QUESTION="What tool is used to manage GPT partitions?"
ANSWER="gdisk for GPT, fdisk for MBR (fdisk also supports GPT in newer versions)"
HINT="fdisk for MBR, gdisk for GPT. Modern fdisk supports both."
